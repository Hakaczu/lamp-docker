# Linux Apache MySQL PHP - Docker-Compose stack
Docker PHP container has installed missing modules like PDO, etc.

## Usage
```bash
git clone https://github.com/Hakaczu/lamp-docker.git
cd lamp-docker
docker-compose up
```
